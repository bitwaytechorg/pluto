import 'package:flutter/material.dart';

class WebMessages extends StatefulWidget {
  const WebMessages({Key? key}) : super(key: key);

  @override
  State<WebMessages> createState() => _WebMessagesState();
}

class _WebMessagesState extends State<WebMessages> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
