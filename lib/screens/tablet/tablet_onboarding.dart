import 'package:flutter/material.dart';

class Tablet_onboarding extends StatefulWidget {
  const Tablet_onboarding({Key? key}) : super(key: key);

  @override
  State<Tablet_onboarding> createState() => _Tablet_onboardingState();
}

class _Tablet_onboardingState extends State<Tablet_onboarding> {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Table onboaring"),);
  }
}
