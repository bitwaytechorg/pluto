import 'package:flutter/material.dart';

class BorderedBtn extends StatelessWidget {
  const BorderedBtn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
